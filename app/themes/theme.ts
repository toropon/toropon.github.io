import { colorsTuple, createTheme } from "@mantine/core";
import { rem, Switch } from "@mantine/core";

const mainColor = "green"

export const theme = createTheme({
    colors: {
        "zaggreen": colorsTuple("#168A4B")

    },

});
